import mon.move as move
import mon.types as types

move.Move(0, "Leafage", types.Types.PLANT, 40, 1.0, move.Categories.PHYSICAL)
move.Move(1, "Bubble", types.Types.WATER, 40, 1.0, move.Categories.PHYSICAL)
move.Move(2, "Ember", types.Types.FIRE, 40, 1.0, move.Categories.PHYSICAL)
